# Shell reference template only. The application does not auto-load this file.
# A disposable local run needs no environment variables.
# Configure exactly one durable signing source when restart-safe evidence is needed:
# export SEMANTIC_LAYER_SIGNING_KEY_FILE=/absolute/path/to/protected/signing-key
# export SEMANTIC_LAYER_SIGNING_KEY=<64-hex-character-key>
